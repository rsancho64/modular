#include "sumres.h"

int suma(int a, int b)
{
    return a + b;
};

int resta(int a, int b)
{
    return a - b;
};

/*/// ----------------------------------
#include <stdio.h>
int main()
{
    int a = 2;
    int b = 3;

    printf(" suma de %d y %d: %d\n", a, b, suma(a, b));
    printf("resta de %d y %d: %d\n", a, b, resta(a, b));
    printf("resta de %d y %d: %d\n", b, a, resta(b, a));
}
//*/// ----------------------------------